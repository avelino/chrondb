name: chrondb
description: Dart/Flutter client for ChronDB — a time-traveling key/value database built on Git architecture.
version: 0.2.3
homepage: https://github.com/avelino/chrondb
repository: https://github.com/avelino/chrondb

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
  path: ^1.8.0

dev_dependencies:
  test: ^1.24.0
